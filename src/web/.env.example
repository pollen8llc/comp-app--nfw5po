# API Configuration
# Core API Gateway endpoint for frontend requests
# Must be HTTPS in production
# Supports v1 API version
NEXT_PUBLIC_API_URL=http://localhost:3000/api/v1

# WebSocket endpoint for real-time updates
# Must be WSS in production
# Required for graph updates and notifications
NEXT_PUBLIC_WS_URL=ws://localhost:3000

# Authentication Configuration - Clerk API v4.0.0
# Public key for frontend SDK
# Required for authentication flows
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=your_clerk_publishable_key

# Secret key for backend operations
# Must be kept secure and never exposed to client
CLERK_SECRET_KEY=your_clerk_secret_key

# Event Platform Integration
# Luma API v1 Configuration
# Required for event imports and synchronization
NEXT_PUBLIC_LUMA_API_URL=https://api.lu.ma/v1
LUMA_API_KEY=your_luma_api_key

# Eventbrite API v3 Configuration
# Required for event imports
NEXT_PUBLIC_EVENTBRITE_API_URL=https://www.eventbriteapi.com/v3
EVENTBRITE_API_KEY=your_eventbrite_api_key

# Partiful API v1 Configuration
# Required for event imports
NEXT_PUBLIC_PARTIFUL_API_URL=https://api.partiful.com/v1
PARTIFUL_API_KEY=your_partiful_api_key

# Social Login Configuration
# LinkedIn OAuth 2.0 Configuration
# Required for social login and profile access
NEXT_PUBLIC_LINKEDIN_CLIENT_ID=your_linkedin_client_id
LINKEDIN_CLIENT_SECRET=your_linkedin_client_secret

# Google OAuth 2.0 Configuration
# Required for social login and profile access
NEXT_PUBLIC_GOOGLE_CLIENT_ID=your_google_client_id
GOOGLE_CLIENT_SECRET=your_google_client_secret

# Environment Configuration
# Valid values: development, staging, production
# Affects security measures and optimization
NODE_ENV=development

# SECURITY GUIDELINES
# 1. Never commit actual .env file to version control
# 2. All production values must use HTTPS/WSS
# 3. Rotate API keys and secrets regularly
# 4. Use strong, unique values for all keys/secrets
# 5. Restrict API key permissions to required scopes only